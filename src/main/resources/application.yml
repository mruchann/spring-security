spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yte-spring-security
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect